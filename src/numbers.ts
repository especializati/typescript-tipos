let example: number;

example = 123; // inteiro
console.log(example);
example = 12.2; // ponto flutuante
console.log(example);
example = 0x37CF; // hexadicimal
console.log(example);
example = 0o377; // octal
console.log(example);
example = 0b111001; // binary
console.log(example);
